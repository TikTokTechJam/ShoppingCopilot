__pycache__/
*.py[cod]
.DS_Store
.env
results.json
data/catalog.jsonl
data/public_set.jsonl
catalog.jsonl.gz
data/releases/
# Organizer-only code, private evaluation data, manifests, and build reports.
organizer/
secure/
# Internal provenance and release-audit working documents.
docs/audits/
docs/data_selection_audit.md
docs/participant_release_checklist.md
# Organizer-only pipeline tests.
tests/test_5core_builder.py
tests/test_organizer_pipeline.py
*.log

# Optional intent-router model weights (~1.2 GB). Fetch with:
#   python -m tools.fetch_reranker
assets/qwen3-reranker-0.6b-onnx

# Local Hugging Face model weights; never commit downloaded encoders.
models/

# Generated annotation datasets and reports.
data/derived/annotations/
data/derived/dictionary/
data/derived/product_embeddings/
data/derived/product_embeddings_jina/

# Local embedding model weights. Download into this path; never commit them.
model/*
!model/.gitkeep
